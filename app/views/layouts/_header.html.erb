<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Mi Historia Clinica", patients_path, id: "logo" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><%= link_to "Pacientes",  patients_path %></li>
      </ul>

      <%= form_tag(patients_path, method: 'get', class: 'navbar-form navbar-left') do %>
          <%= search_field_tag 'term', nil, class: 'form-control', placeholder: 'Buscar paciente'  %>
          <%= submit_tag "Buscar", class: 'btn btn-default', :name => nil %>
      <% end %>

      <ul class="nav navbar-nav  navbar-right">
        <% if logged_in? %>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                <%= current_user.email %> <b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to "Cambiar contraseña", edit_password_user_path(:id => current_user) %></li>
                <li class="divider"></li>
                <li>
                  <%= link_to "Salir", logout_path, method: :delete %>
                </li>
              </ul>
            </li>
        <% else %>
            <li><%= link_to "Ingresar", login_path %></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>